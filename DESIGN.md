# DESIGN — 张馨丹个人网站

## 1. 页面目标
- 面向产品经理 / AI 产品经理招聘方，90 秒内建立“能理解复杂系统、能把机制落成产品、能用数据持续优化”的候选人认知。
- 主操作：阅读代表案例；次操作：邮件联系。
- 静态单页（HTML + 内嵌 CSS + 原生 JS，无构建依赖），可直接部署至 GitHub Pages；桌面与移动端均可读。

## 2. 视觉命题
“暖调编辑室”：米白纸感底色 + 姜黄与霜蓝双色强调，衬线大标题配等宽标注，表达“理性但有人味”的产品经理形象。信息以编号案例、手风琴项目列表、杂志式相册组织，避免模板化的等大卡片墙。

## 3. 颜色角色
- Canvas `#FBF6EC`：米白主背景。Canvas-warm `#F4EAD6`：暖米区块底。
- Ink `#1D2B33`：主文字。Ink-soft `#3A4D57`、Muted `#6C7A83`：说明文字。
- Line `#E0D4BF` / Line-strong `#C9B48F`：结构线。
- Gold `#C98A1B`：主强调（操作、编号、悬停）。Gold-dk `#A66F0F`：hover 态。Gold-soft `#F0D59B`：深色底上的强调。
- Peacock `#4C7FA8`：次强调（标题强调字、跑马灯、contact 底）。Peacock-dk `#1F3348`：深色章节底。
- Clay `#B3532A`：备用暖色，少量点缀。
- 效果 token：`--shadow-peacock` 人像投影、`--halo-gold` 暖金光晕。颜色只允许经 `:root` token 引用，不新增硬编码色值。

## 4. 字体
- 展示：Cormorant Garamond / Songti SC / Noto Serif SC / Georgia，承担编辑感。
- 正文：Inter / PingFang SC / Microsoft YaHei，承担清晰与效率。
- 功能文字：等宽字体，承担编号、日期与状态标注。
- 标题一律正体（roman），强调用颜色（浅色底 Peacock、深色底 Gold-soft）；斜体只用于正文引语。

## 5. 布局
- 最大宽度 1240px。桌面 Hero 为 1.15 / 0.85 左右分栏。
- 代表工作：88px 编号 + 6/4 双栏案例行（深色章节）。
- 能力组合：列表行（编号 / 标题 / 描述三列），发线分隔，不使用卡片。
- 相册：杂志式版式（封面故事大图 + 两张专题小图 + 方格网格），摄影与美食两个 tab 共用。
- 960px 以下改单列；移动端导航收敛为菜单按钮，正文不小于 15px。

## 6. 标志性手法
- 剪纸拼贴 Hero：金色剪纸多边形 + 抠图人像 + 浮动标签 + 虚线弧。
- 衬线跑马灯 strip（装饰性，aria-hidden）。
- 手风琴“项目选集”：一级只露角色与结果，展开看方法。
- 页脚城市路径：东北 → 南京 → 成都 → 北京 → 上海。

## 7. 交互与动效
- 仅首屏做一次性编排入场（0.7s 错峰上浮）；其余内容进视口不再附加动效。
- 链接和按钮有 hover、active、focus-visible 状态；hover 只做颜色或轻微位移。
- 遵守 prefers-reduced-motion：入场、跑马灯、hover 位移全部关闭。
- 生活区 tabs 支持点击与方向键切换；移动端菜单使用原生 button，支持键盘和 Escape 关闭。
- 层级使用命名 z-index 刻度（--z-paper / arc / cutout / scroll / tag / header / skip）。

## 8. 内容与隐私
- 只使用简历中的可验证经历，不虚构客户评价或项目数据。
- 公开页面不展示手机号、年龄、政治面貌；保留工作邮箱作为联系方式。
- 文案以结果和方法为主，避免“热爱、赋能、全栈型”等空泛标签。

## 9. 验收
- 无控制台报错；360×800、768×1024、1440×900、1920×1080 无横向溢出。
- 200% 缩放可读，键盘焦点可见，减少动态效果下内容完整，打印样式可用。
- 本文件与实现保持一致：修改设计实现时必须同步修改本文件。
